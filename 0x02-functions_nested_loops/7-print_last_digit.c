#include "main.h"

/**
 * print_last_digit - prints last digit of number
 * @Jay: last digit
 * Return: value of last digit
 */
int print_last_digit(int jay)
{
	int xyz;

	xyz = (jay % 10);

	if (xyz < 0)
	{
		xyz = (-1 * xyz)
	}
	_putchar(pld + '0');
	return (xyz);
}
